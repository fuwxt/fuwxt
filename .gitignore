# Editor / OS junk
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# This site is published via plain GitHub Pages with `.nojekyll`,
# so Jekyll/Bundler cache directories never appear here. Removed
# the previous Jekyll-specific patterns (_site/, .sass-cache/,
# .jekyll-cache/, /vendor, Gemfile.lock) — they were dead weight.
